# 环境档案（<知识库名>）

> 由 tutor-kb-loop 首次运行时按本模板生成，落在 `<库>/tutor/loop/environment.md`。
> **定位**：本环境的**数据快照**，不是流程逻辑。读它只影响「去哪找、已知什么坑」，不改变任何执行步骤。
> **随库走**：与 `ledger.md` / `log.md` 同级，不随画像迁移而丢失。
> 参考填法见 `examples/`。

## 目标对

| 导师组 | `<库>` | `<画像根>` | 布局模式 | 可写性 | 状态 |
|:--|:--|:--|:--|:--|:--|
| _导师组名_ | _知识库根路径_ | _画像目录路径_ | 库内 / 库外 / 混合 | 库可写 / 画像只读 | ✅ 可跑 / ⚠️ 部分受限 / ❌ 不可用 |

## 导师包状态

| 包 | 文件数 | 判定 | 一手 PDF（L1/L2） | L3 可抽条数 | 备注 |
|:--|:--|:--|:--|:--|:--|
| _[person]-perspective_ | — | 完成 / 薄 / 骨架 | — | — | — |

> 判定口诀：**先判有无 `SKILL.md`，再判底稿厚薄**（有 SKILL.md 但无代表论文清单且无 PDF = 薄包）。

## 第五路基线（覆盖率核算用）

| 项 | 值 |
|:--|:--|
| 采用的回退层级 | L1 / L2 / L3 / 不可核算 |
| 待核条目合计 | — |
| `wiki/sources/` 已入库页数 | — |
| 当前覆盖率 | — % |

## 受限 / 不可访问目录

| 目录 | 现象 | 处理 |
|:--|:--|:--|
| — | 列目录被拒 / 只读 / 不存在 | Step 0 跳过并标注 / 写回落 pending |

## 领域专属防错清单

> 与 SKILL.md 的「通用防错清单」合并生效。记录**本环境本领域的否定性结论**——这类资产最容易随人员变动丢失。

| 结论 | 影响 | 动作 | 日期 |
|:--|:--|:--|:--|
| _如：某关键论文已撤稿_ | _哪些口径不可信_ | _引用降级 / 建否定性 source 页 / 不补_ | — |
| _如：某数据集无公开数据_ | _永久缺口_ | _标 `declined`，不重复投入_ | — |

## 历史轮次索引

| 轮次 | 日期 | 目标对 | 关键产出 | 收敛判定 |
|:--|:--|:--|:--|:--|
| R## | YYYY-MM-DD | — | — | 未收敛 / ✅ 已收敛 |
