---
name: tutor-kb-loop
version: 0.3.1
description: 导师画像×知识库互补循环优化（环境无关，支持多库目标对）：确定「导师组↔知识库」目标对→缺口审查→下载→转换→消化→反馈写回画像，螺旋收敛。当用户要求「跑一轮互补循环」「缺口审查」「知识库还缺什么文献」「文献缺口台账」「收敛判定/循环状态」「下载文献 X」「反馈写回画像」「切换目标对/用哪个知识库」时使用。编排于 llm-wiki 之上，消费其消化/查询/状态能力并回写导师画像。
---

# tutor-kb-loop — 知识库 × 导师视角 互补循环

## 核心理念

- **导师视角 = 脑**：导师画像定义领域判断标准与理想态（该有什么基因/路线/数据/缺口）。
- **知识库 = 体**：提供实证事实与数据（哪些发现验证过、哪些带置信度标注）。
- **互为检验**：视角缺事实支撑会飘，事实缺视角组织会散。
- **循环**：视角定义理想态 → 缺口审查找差距 → 补文献 → 新事实反馈写回画像 → 视角锐化 → 找新差距 → 螺旋收敛。

**一句话定位**：导师视角定义领域「应该有什么」，知识库提供「有什么」；差距即文献缺口，补齐后反哺视角，视角锐化后再找新差距——两者互相喂养、螺旋收敛。

> **v0.3 变更要点**：剥离全部环境/领域硬编码，主文档为**纯通用方法论**；新增「环境档案」机制（可选，按需生成），承载目标对清单、包状态、领域防错项等环境特定信息；原实测基线、示例轮次、领域防错项下沉至 `examples/`。

## 依赖 skill / 工具

| 依赖 | 用途 | 回退方案 |
|------|------|---------|
| `llm-wiki` | Step 4 消化（ingest/batch-ingest）、知识库定位（`.wiki-schema.md` / `~/.llm-wiki-path`） | 直接按 wiki schema 手动消化 |
| 文献检索/下载（如 paper-search MCP / WebSearch） | Step 2 下载全文 | 用户手动提供 PDF |
| anydoc（`llm-wiki` 内置 venv，路径由 llm-wiki 提供；Windows 常见 `~/.venvs/anydoc/Scripts/python.exe`，macOS/Linux `~/.venvs/anydoc/bin/python`） | Step 3 PDF→Markdown（表格还原优先） | `markdown-converter`（markitdown） |
| `nuwa-skill`（可选） | 建库前置：新领域蒸馏 Top5 导师画像；骨架包「待蒸馏」引导 | 跳过蒸馏，用现有导师包 |

## 环境档案（可选，推荐）

主文档刻意不含任何具体路径、领域名、项目名——**不同使用者的知识库布局、导师包位置、领域防错项都不同**。环境特定信息记入**环境档案**，与状态区一起随库走：

```
<库>/tutor/loop/environment.md   # 按 templates/environment-template.md 生成，懒初始化
```

**档案内容**：本环境的可用目标对、导师包状态（完成/薄/骨架）、第五路可抽量基线、受限或无权限目录、**领域专属防错清单**、历史轮次索引。

**使用约定**：
- 跑循环前先读它 → 省掉每轮重复摸底；不存在则跳过（不阻塞），首次 `loop`/`gap-audit` 后可建议用户补建。
- **档案是环境数据，不是流程逻辑**——读它只影响「去哪找、已知什么坑」，不改变任何执行步骤。
- 领域性否定结论（撤稿、数据不可得等）记入档案的防错清单，与下方「通用防错清单」合并生效。

---

## Step 0｜确定目标对（必做，不默认）

**一对 = 一个导师组（画像根） ↔ 一个知识库（库根）。** 不同环境下画像可能在库内、库外或两者混合，故一律动态发现，不预设布局。

### 两个根变量（后续全文 `<库>` / `<画像根>` 均由此步确定）

| 变量 | 含义 | 落点 |
|:--|:--|:--|
| `<库>` | 知识库根（循环围绕它跑，状态区在此） | 由用户指定或按 llm-wiki 定位逻辑确定 |
| `<画像根>` | 导师画像所在目录（**只读数据源**） | 库内 `<库>/tutor/` 或库外独立目录（见布局模式） |

> **状态区始终随库走**：`tutor/loop/`、`tutor/board.md` 建在 `<库>/tutor/` 下（懒初始化），与 `<画像根>` 是否分离无关。这样循环记录跟着被优化的对象走，不会因为画像换位置而丢失。

### 发现顺序（取第一个命中）

1. **用户显式指定**："用 XX 库跑一轮" / "对 YY 做缺口审查"。
2. **环境档案**：读 `<库>/tutor/loop/environment.md` 的目标对清单（若存在）。
3. **动态扫描**（多候选位置**同时**扫，合并去重，**不做二选一**）：
   - A **库内**：`<库>/tutor/*-perspective/`
   - B **向上扩展**：以 `<库>` 为起点**向上 1~2 级**作为搜索根，向下 **1~3 层**广度搜 `*-perspective/`（通配层数从 1 起，否则 `<根>/<画像目录>/<包>` 这类两层结构会漏）
     - 真实布局千差万别：画像可能在 `tutor/<库名>/`、也可能在**异名**目录（如 `Mytutor/<导师组名>/`、`tutors/`、`advisors/`）。**硬编码任何目录名都会漏**，故只认 `*-perspective` 这一稳定特征
   - C **排序推荐**：①父目录名 == 库名 → ②祖先路径含库名 → ③位于导师类目录（名字不固定，按 `tutor`/`tutors`/`advisor`/`mentor` 等模糊匹配）→ ④其余按距 `<库>` 路径由近及远
   - **权限容错**：读取被拒（无权限/不存在）→ 跳过并在输出中标注，不中断扫描
4. **询问用户**：列出扫描到的候选对让用户选。**不得擅自假定。**

### 常见布局模式

| 模式 | 特征 | `<画像根>` | 处理 |
|:--|:--|:--|:--|
| **库内** | 导师包在库里 | `<库>/tutor/*-perspective/` | 最直接；注意 `<库>/tutor/` 同时是状态区，勿把状态文件误当导师包 |
| **库外** | 导师包独立于库（多库共享一组导师时常见） | `<某根>/<画像目录>/<库名或导师组名>/*-perspective/`（**目录名不固定**，靠向上扩展扫描命中） | `<画像根>` 常**只读**（共享盘/他人维护），写回走 pending 分支 |
| **混合** | 库内库外都有 | 两处合并去重 | 逐个判完成/薄/骨架；同名包以库内为准并提示 |
| **无导师包** | 库下无任何 `*-perspective/` | — | 走建库前置（nuwa-skill 蒸馏 Top5），或**借用方向最接近的单导师**并在台账标注覆盖度打折 |

> **多库切换**：每次独立触发都重跑 Step 0。同一会话内已确定则沿用，除非用户说"换库/切换目标对"。

### 建库前置：新领域蒸馏 Top5（已有导师则跳过）

1. 联网检索该领域成果最硬、路线覆盖最全的 Top5 领军人物。
2. 用 `nuwa-skill` 每人蒸馏为 `<name>-perspective/SKILL.md`（核心心智模型 / 决策启发式 / 表达DNA + 诚实边界）。
3. 已有对应导师 → 跳过，直接进 5 步循环。
4. **理由**：Step 1 缺口审查需要画像的「理想态」作对照；没有导师的库，循环无从驱动。

## 工作流总览

**不触发**：仅要求"总结这篇文章"、"查询某概念"（走 llm-wiki）。

| 用户意图关键词 | 工作流 |
|---|---|
| "跑一轮互补循环" / "执行一轮" | **loop**（Step 0 + 主循环 5 步串行） |
| "缺口审查" / "知识库还缺什么文献" | **gap-audit**（Step 1 单独执行） |
| "下载文献 X" / "补这篇文献" | **fetch**（Step 2+3） |
| "反馈写回画像" / "更新导师画像" | **feedback**（Step 5） |
| "循环状态" / "收敛了吗" / "台账" | **status**（读台账+轮次日志） |
| "切换目标对" / "用 XX 库" | **pairs**（仅跑 Step 0，输出候选并确认） |

---

## 通用前置检查（除 status、pairs 外均先执行）

1. **Step 0 确定目标对** → 得到 `<库>` 与 `<画像根>`
2. **定位知识库**（复用 llm-wiki 逻辑）：
   - 当前工作目录含 `.wiki-schema.md` → 用它作为知识库根路径
   - 否则读 `~/.llm-wiki-path`
   - 两者都没有 → 提示用户先运行 llm-wiki 的 init，或直接给出知识库路径
   - **与 Step 0 结果冲突时，以用户显式指定为准，并回读确认**
3. **读取 `.wiki-schema.md`** 确认语言（`语言：中文`/`语言：English`，缺省 zh），输出语言随库语言
4. **读环境档案**（`<库>/tutor/loop/environment.md`，若存在）→ 取已知目标对、包状态、领域防错项
5. **懒初始化**（首次运行自动补建，不阻塞流程，完成后告知用户）：按「文件落位表」检查，缺失即补建 `<库>/tutor/loop/`、`board.md`、`raw/pdfs_original/`、`raw/pdfs_md/`、`wiki/reviews/`
6. **扫描导师包**（扫 `<画像根>/*-perspective/`，若与 `<库>/tutor/` 不同则两处都扫、去重）：
   - **完成包**：有 `SKILL.md` 且 `references/` 下有 `01-writings.md` 的「代表性论文」清单或 PDF → 缺口审查可正常驱动
   - **薄包**：有 `SKILL.md` 但**既无代表性论文清单、也无任何 PDF**（典型：包内仅数个文件）→ 第五路不可核算；**须在台账登记一条「导师包调研底稿待补」P1 画像侧缺口**，可引导 nuwa-skill 补蒸馏
   - **骨架包**：无 `SKILL.md`（只有调研底稿）→ 标「待蒸馏」，P2 观察项，不阻塞循环
   - 判定口诀：**先判有无 SKILL.md，再判底稿厚薄**——只看 SKILL.md 会把薄包误当完成包，导致第五路静默空转
   - 导师包 **只读取、不激活、不复制注册**（避免双份漂移）

---

## 字段映射表（流程文档概念 → 现状画像字段）

> 流程文档引用的部分字段在 nuwa-skill 画像中不存在，按此表映射；不存在且无映射的落 `board.md`。

| 流程文档概念 | nuwa-skill 画像实际字段 | 落点 |
|:--|:--|:--|
| 科研思维 / 学术决策 / 表达风格 | 核心心智模型 / 决策启发式 / 表达DNA | 个人画像 SKILL.md |
| 关键数据锚点（部分） | 核心心智模型下的证据、决策启发式下的案例 | 个人画像对应章节 |
| 技术路线×矩阵 | （无）→ 新建 | `<库>/tutor/board.md` |
| 交叉视角 | 智识谱系（部分承载） | `<库>/tutor/board.md` |
| 工作清单 | （无）→ 建议 nuwa-skill 扩展 | `<库>/tutor/board.md`（当前落点） |
| 诚实边界 | 诚实边界（✅ 存在） | 个人画像 SKILL.md 末尾 |
| 时间线 | 人物时间线（✅ 存在） | 个人画像 SKILL.md |
| 联合画像 + 加载器 | （无此机制；WorkBuddy 单次激活一个 skill） | `<库>/tutor/board.md`（数据文件，不激活） |

> **扩展建议（不落地，仅提议）**：nuwa-skill 的 `skill-template.md` 未来可增「关键数据锚点」「工作清单」两节，届时字段映射表可简化。

---

## 工作流 1：loop（主循环）

触发："跑一轮互补循环"。执行顺序：

1. **通用前置检查**（含 Step 0、读环境档案、懒初始化、扫描导师包）
2. **Step 1｜缺口审查** → 走 gap-audit 工作流，产出/更新 `<库>/tutor/loop/ledger.md`。**完成判据**：台账每条含完整字段（ID/文献标识/归属导师方向/为什么需要/来源可追溯性/优先级/状态），五路来源均已读取
3. **Step 2+3｜下载 + 转换** → 走 fetch 工作流，逐条处理台账中 `open` 项。**完成判据**：台账中所有 `open` 项已推进（`downloaded`/`converted`）或标 `blocked`/`declined`
4. **Step 4｜消化** → 走 llm-wiki 的 ingest/batch-ingest（原文与转换稿落位 `raw/pdfs_original/`、`raw/pdfs_md/` 后，把 `raw/pdfs_md/` 下文件喂给 llm-wiki），产物进 `wiki/sources/`，置信度沿用 llm-wiki 标准（EXTRACTED/INFERRED/AMBIGUOUS/UNVERIFIED）。**完成判据**：所有 `converted` 项已入库，`wiki/sources/` 有对应页
5. **Step 5｜反馈写回** → 走 feedback 工作流。**完成判据**：本轮所有 ingested 的新事实已按归属路由写回，`tutor/loop/feedback-log.md` 已逐条登记
6. **收敛判定** → 读 `tutor/loop/log.md` 末尾 2 条：连续 2 轮无新增 P0/P1 + P2 全部显式处置 + **导师核心文献覆盖率达标**（每位完成包导师代表论文 ≥ 1 篇入库；覆盖率可核算时 ≥ 30%，未达标视为未收敛，应转 Gap 批量补全条目）→ 输出「已收敛，转低频维护」；否则输出「未收敛」及剩余项
7. **登记轮次** → 按 `templates/log-template.md` 追加本轮记录

---

## 工作流 2：gap-audit（缺口审查）

触发："缺口审查" / "知识库还缺什么文献"。**输出**：更新 `<库>/tutor/loop/ledger.md`。

### 缺口来源（五路合并，缺一不可）

1. **导师画像理想态**：读 `<画像根>/*-perspective/SKILL.md`（只读完成包）的「核心心智模型 / 决策启发式 / 时间线 / 价值观与反模式 / 智识谱系 / 诚实边界」+ `references/research/01-writings.md`、`05-decisions.md`（若存在）
2. **库内「待补」标注**：Grep `<库>/wiki/` 内 `[待创建`、`confidence: AMBIGUOUS`、`confidence: UNVERIFIED`；读 `<库>/wiki/reviews/` 下 `status: pending` 项；顺带读库根 `purpose.md` / `index.md` / `overview.md` 的显式缺口段
3. **已知最大缺口**：读 `tutor/loop/ledger.md`（遗留未处理项）+ `tutor/loop/log.md`（最近轮次）
4. **用户当前问题**：本次调用想解决什么、补什么
5. **导师核心文献覆盖率**（弥补"循环只补边界、不补文献存量"短板）：**三级回退取待核清单**（因多环境实测一手 PDF 常缺失）：
   - L1：`<画像根>/*-perspective/references/sources/articles/*.pdf`（最理想）
   - L2：递归 `<画像根>/*-perspective/references/**/*.pdf`（任何位置的 PDF 都算）
   - L3：**无 PDF 时**（多环境常见），从调研底稿抽取代表论文清单，**按以下顺序取，勿用笼统正则**：
     1. `references/research/01-writings.md` 的 **`## 代表性论文` 表格**（列形如 `年份 | 期刊 | 标题 | 核心贡献`）→ 逐行取「年份+期刊+标题」；**先定位章节，再按该文件实际列顺序解析**，不要假定固定列位
     2. 同文件「核心研究方向」各节的**行内线索**（模式：`<主题>（<说明>）：<4位年份> <期刊>`）
     3. `06-timeline.md` 的成果类行（非教育/行政/荣誉行）
   - **坑**：表格里的年份**常不带括号**，用 `\((19|20)\d{2}\)` 类正则会全军覆没——必须先找 `## 代表性论文` 章节，正则只作兜底
   - **坑**：部分包压根没有「代表性论文」章节，L3 抽 0 条属**薄包底稿缺失**，不是"该导师无论文"——别据此判「不可核算」了事
   - 逐级与 `<库>/wiki/sources/` 比对（按**标题/DOI/作者+年份特征词**，不能只看文件名——库有改名/占位/截断教训）
   - **未入库的导师代表论文 → 合成一条 `GapNNN`（P0，核心文献补全）**，附子项清单与缺口数（编号规则见「台账条目」）。此路是循环文献增量的主要来源
   - **为什么合成一条而非逐篇登记**：这批文献①**同源同质**——根因只有「导师核心文献存量未建」一个，不是 N 个独立决策缺口；②**同批动作**——批量下载→批量转换→批量消化，状态同步推进。若逐篇登记，P0 计数会瞬间虚高（如 40 条），直接把「连续 2 轮无新增 P0/P1」的收敛门槛顶死，还会把**结构性存量缺口**伪装成 40 个离散缺口，掩盖真正的问题
   - L3 模式下覆盖率的分母 = 抽出的代表论文条数；**L1–L3 全空 → 分两种情况**：①薄包 → 登记 P1 画像侧缺口（补蒸馏底稿），覆盖率标「不可核算（底稿缺失）」；②完成包但确无清单 → 标「不可核算」，收敛判定中该项降级为提示项。两种都不阻塞循环

### 审查方法

1. 逐条对照画像的「核心心智模型 / 决策启发式」问：库里有没有对应 source 支撑？（用**内容特征词**匹配库内 source，不能只看文件名——库有文件名截断/占位件教训）
2. 对照 `board.md`（若无则对照 `<库>/index.md` 或 tutor README）问：每个导师方向是否都有文献覆盖？哪块最薄？
3. 对照「诚实边界」问：已知缺口补上了吗？
4. 对照「导师核心文献覆盖率」（第五路）：每位导师的代表论文有几篇已入库？缺失清单即批量台账条目
5. 合并五路结果，去重（同一文献多路命中只记一条，标注多归属）

### 台账条目（按 `templates/ledger-template.md`）

> **编号 `GapNNN`** = **Gap（文献缺口条目）** + 三位序号，如 `Gap001`、`Gap002`。规则：
> - **三位补零**——不补零的话，`Gap10` 会排在 `Gap2` 前面（字符串排序），台账一破百条就乱
> - **全局递增、关闭后不回收号**——历史 log 里引用过，`Gap007` 关了就永远是 Gap007，不留给新条目
> - 一轮内按发现顺序续号，不按优先级重排（ID 只表示发现先后，不表示重要性）

每条含：ID | 文献标识（作者+年份+标题/主题）| 归属导师方向（领域整体/具体导师）| 为什么需要（补哪个锚点/交叉视角/清单项）| 来源可追溯性 | 优先级 P0/P1/P2 | 状态

- **来源可追溯性**：A=检索到原文 / B=库内推断缺 / C=仅凭模型记忆（C 级默认降为 P2，除非用户确认）
- **优先级**：P0=影响关键结论/决策 → P1=影响执行质量 → P2=打磨/低价值
- **批量条目**（一条装多篇，第五路产出专用）：**必须带「子项完成度 x/y」**——状态机只能整体推进，没有这个字段就无法表达「40 篇消化了 15 篇」的中间态。子项清单写在条目的明细表里
  - 单篇条目该字段填 `-`
  - 子项推进到 `y/y` 时，整条才允许置 `closed`
- 骨架包对应的方向：在台账标注「待蒸馏（导师包无 SKILL.md）」为 P2 观察项，不阻塞循环

**完成判据**：五路来源全部读取；台账每条含完整字段；无未分类的缺口（每条至少归入一个导师方向 + 一个优先级）；**每位完成导师包的覆盖率已核算并写入台账**（可核算时 <30% 或代表论文缺失须登记批量补全条目；不可核算时显式标注原因）。

---

## 工作流 3：fetch（下载 + 转换）

触发："下载文献 X" / loop 中 Step 2+3。对台账中每条 `open` 项：

1. **检索**：用文献检索/下载工具（paper-search MCP 等，优先于 curl 直下——实测 MCP 通道比直连稳定）获取全文；付费墙尝试替代通道（OA 版本/预印本/作者主页）
2. **验证**：文献标识必须实际检索到（标题+作者+年份匹配），**不靠模型记忆**；检查文件魔数（`%PDF` 等）；**PDF 下载失败/截断（魔数 OK 但 pypdf 报 Stream ended / 0 字节）→ 不再死磕，直接转 WebSearch 抓官方全文+摘要重消化**（实测有效：预印本站点 429、部分出版商返 0 字节、导师包多包 PDF 截断均以此兜底成功）
3. **落位**：原文 → `<库>/raw/pdfs_original/`；文件名合规（去冒号等非法字符）
4. **转换**：优先 anydoc（venv 路径见依赖表）转 Markdown；扫描件需 OCR（不支持则标 `blocked(OCR)`）；老 PDF 内嵌字体提取失败用 PyMuPDF `get_text()` 兜底；转换后抽查无乱码/截断
5. **落位**：转换稿 → `<库>/raw/pdfs_md/`；统一 UTF-8 编码
6. **更新台账**：状态机推进；失败标 `blocked` + 原因（付费墙/OCR/无法获取），**受阻项不算未收敛**。**完成判据**：本条 `open` 项已推进（`downloaded`/`converted`）或标 `blocked`/`declined`

---

## 工作流 4：feedback（反馈写回画像）

触发："反馈写回画像" / loop 中 Step 5。消化出的**新事实**按归属写回。**约束**：只补**带来源的事实性更新**，不改导师的**思维模型本体**。

### 归属路由（防跨导师组写回冲突）

| 反馈内容 | 写回位置 |
|:--|:--|
| 导师本人的新成果 / 新数据 | 该导师个人画像 `<画像根>/[person]-perspective/SKILL.md` |
| 领域整体的新路线/新数据/新共识 | 联合画像 `<库>/tutor/board.md`（矩阵/交叉视角/工作清单/诚实边界） |
| 新发现的领域缺口 | `<库>/tutor/board.md` 的工作清单/诚实边界（作为下一轮 Step 1 起点） |

- 导师可能同时属于多个导师组（多对多）：写回前先检查该导师归属；**同一事实只落一处**，不复制
- **个人事实 → 个人画像，不写联合画像**；**跨组解释/领域判断 → 才写联合画像**
- 个人画像具体映射：新成果 →「人物时间线」追加；新证据 → 对应「核心心智模型」补充证据（不改模型名与定义）；新决策案例 →「决策启发式」追加案例；新局限 →「诚实边界」追加
- **`<画像根>` 只读时**（共享盘/他人维护场景常见）：写回前先探测可写性，不可写则**把待写回内容完整落到 `<库>/tutor/loop/feedback-log.md` 并标 `pending`，提示用户手动同步**，不静默失败、不擅自改路径

### 思维模型本体保护

- 模型**名**与**一句话定义**的改动 → 需**强证据 + 人工确认**（强证据 = 一手来源直接陈述其思维/定位变化，如本人访谈/著作原文）
- 证据、应用案例、时间线条目 → 可增补，改动处加注释：
  `<!-- 来源: wiki/sources/<页> | confidence: EXTRACTED | 轮次: R## -->`

### 反馈日志（可追溯性）

每次写回按 `templates/log-template.md` 中的反馈日志表登记到 `<库>/tutor/loop/feedback-log.md`（写回文件 / 改动节 / 补入内容摘要 / 来源 source / 日期·轮次）。

---

## 工作流 5：status（状态与收敛判定）

触发："循环状态" / "收敛了吗"。**只读**，不改文件：

1. 读 `<库>/tutor/loop/ledger.md`：按 P0/P1/P2 计数、按状态机计数（open/downloaded/converted/ingested/fedback/closed/blocked/declined）
2. 读 `<库>/tutor/loop/log.md` 末尾 2 条轮次记录
3. 输出报告：
   - 剩余缺口清单（P0/P1 优先，含 ID/文献标识/状态/受阻原因）
   - **收敛判定**：连续 2 轮无**新增** P0/P1 + P2 全部显式处置（closed/blocked/declined）+ **导师核心文献覆盖率达标**（每位完成包导师代表论文 ≥1 篇入库；可核算时 PDF 覆盖率 ≥30%；不可核算时降级为提示项并说明）→ ✅ 已收敛，转低频维护；否则 ❌ 未收敛（未达标项列出）
   - 骨架包/薄包提醒（若有）
4. 若台账不存在 → 提示先跑 gap-audit

---

## 工作流 6：pairs（目标对发现，只读）

触发："切换目标对" / "用哪个知识库" / "有哪些可用对"。只跑 Step 0：

1. 按 Step 0 第 3 步的多候选位置扫描 `*-perspective/`（库内 + 向上扩展），无权限目录跳过并标注
2. 与**环境档案**交叉，输出候选对表：`导师组 | <库> | <画像根> | 布局模式 | 完成包数/薄包数/骨架包数 | 状态`
3. 标注 `<库>/tutor/loop/` 是否已有历史（有无 `log.md`、有无 `environment.md`）
4. 让用户选；**不得擅自假定**

---

## 文件落位表

```
<库>/                                    # 知识库根（本 skill 唯一可写区）
├── .wiki-schema.md                      # llm-wiki 库标识（定位依据）
├── purpose.md / index.md / overview.md  # 库规则：只读区、不装什么、引用格式（Step 1/4 必读）
├── tutor/                               # 【状态区，随库走】
│   ├── board.md                         # 【懒初始化，按 templates/board-template.md】联合画像：领域共识矩阵/技术路线×方法矩阵/交叉视角/工作清单/诚实边界
│   └── loop/
│       ├── environment.md               # 【可选】环境档案（目标对/包状态/领域防错项/轮次索引）
│       ├── ledger.md                    # 【懒初始化】缺口台账
│       ├── log.md                       # 【懒初始化】轮次记录（含收敛判定）
│       └── feedback-log.md              # 【懒初始化】反馈日志（含 <画像根> 只读时的 pending 项）
├── raw/
│   ├── pdfs_original/                   # 【懒初始化】文献原文 PDF
│   └── pdfs_md/                         # 【懒初始化】文献转换 Markdown
├── wiki/reviews/                        # 【懒初始化】llm-wiki 待审项（确认后单向入台账）
└── wiki/sources/                        # llm-wiki 消化产物（复用，不另建）

<画像根>/                                # 可能在库内，也可能完全独立（只读）
└── [person]-perspective/
    ├── SKILL.md                         # 只读数据源（不激活）
    └── references/
        ├── research/                    # 01-writings / 02-conversations / 05-decisions / 06-timeline …
        └── sources/articles/*.pdf       # 【可选】一手 PDF，第五路 L1 来源；缺失走 L3
```

> `<画像根>` 可能与 `<库>/tutor/` 是同一目录（库内布局），也可能完全分离（库外布局）。**状态区永远在 `<库>/tutor/`**，画像永远只读。

## 状态机

```
open → downloaded → converted → ingested → fedback → closed
            │             │           │
            └── blocked（付费墙/OCR/无法获取，标原因）
            └── declined（用户判定不追）
```

- `blocked` / `declined` 均视为「显式处置」，参与收敛判定但不要求闭环到 closed
- 台账 ↔ llm-wiki review 机制为**单向衔接**：review 确认后入台账；台账不反向写 review

---

## 通用防错清单（每次 Step 1/2/4 前必扫）

> 以下为**跨环境通用**的工具/流程类陷阱。领域专属的否定性结论（如某论文已撤稿、某数据集不可得）记在**环境档案**里，与本表合并生效。

| 结论 | 影响 | 动作 |
|:--|:--|:--|
| **文献镜像站普遍被人机验证/反爬拦截** | 付费墙文献走不通 | 直接转 OA/预印本/作者主页；再不行转 WebSearch 抓官方全文+摘要重消化，不要反复重试镜像 |
| **预印本站点/出版商常返 429 或 0 字节** | 下载假成功 | 验魔数 + 用 pypdf 打开；截断即转 WebSearch 兜底，不死磕 |
| **"空"目录可能是历史批量归档事故** | 误判为缺口 | 审查前先判目录真实性，别把归档事故当文献缺口 |
| **文件名会截断/改名/占位** | 覆盖率比对失真 | 一律用标题/DOI/作者+年份特征词匹配，禁止只比文件名 |
| **共享盘/网络盘上回收站式删除会失败** | 清理操作假成功 | 需要真删时用 `[System.IO.File]::Delete()` 一类直接 API，并验证 |
| **只读目录写入静默失败** | 反馈写回丢失 | 写前探测可写性；不可写落 `feedback-log.md` 标 `pending` 并提示 |

---

## 变更记录

- **2026-08-17 v0.1.0**：自团队内部旧版 `kb-optimize-loop` 派生，改为 WorkBuddy 单文件自包含；路径动态定位（`.wiki-schema.md` → `~/.llm-wiki-path`）；置信度改用 llm-wiki 标准；新增字段映射表与实测兜底策略。
- **2026-08-31**：缺口来源增第五路「导师核心文献覆盖率」；收敛判定加覆盖率 ≥30% 门槛。
- **2026-09-03 v0.2.0**（合并环境实测修正）：
  1. 新增 **Step 0 目标对发现**，支持多库切换；双通道扫描（库内 `tutor/` + 库外同级 `tutor/<库名>/`），修掉 v0.1「画像必在库内」的假设错误。
  2. 新增 **示例轮次记录**与**否定性结论防错清单**，把撤稿、镜像失效、归档事故等防错资产随技能走。
  3. 新增 **工作流 6 pairs**（目标对发现，只读）。
  4. 第五路覆盖率扫描加 **L1/L2/L3 三级回退**；全空时显式标注「不可核算」并降级为提示项，不阻塞循环。
  5. 明确 **状态区随库走**、**画像永远只读**；`<画像根>` 不可写时落 `feedback-log.md` 标 `pending`。
  6. 落位表补 `purpose.md / index.md / overview.md` 为 Step 1/4 必读项；补充建库前置。
  7. 导师包判定加 **「薄包」中间态**（有 SKILL.md 但底稿无代表论文清单且无 PDF）。
- **2026-09-03 v0.3.0**（环境无关化重构）：
  1. 剥离全部环境硬编码（具体盘符/路径/项目名/领域名），主文档改为**纯通用方法论**；依赖表路径改为按平台推断。
  2. 适用矩阵 → **布局模式**（库内/库外/混合/无导师包）+ **权限容错**规则；Step 0 增加「环境档案」发现通道。
  2.1 **修掉 v0.2 的残留环境耦合**：v0.2 的库外扫描写死「同级 `tutor/<库名>/`」，照抄了单一环境长相，遇到异名目录（`tutors/`、`Mytutor/`）必漏。改为**向上 1~2 级 + 向下 1~3 层**广度搜 `*-perspective/`，只认包名特征、不认目录名。4 种布局冒烟验证通过（库内 / 库外 `tutors` / 库外 `Mytutor` / 无导师包）。
  3. 新增 **环境档案**机制（`<库>/tutor/loop/environment.md` + `templates/environment-template.md`）：承载目标对清单、包状态、第五路基线、**领域专属防错清单**、轮次索引。
  4. 防错清单拆为**通用防错**（主文档，工具/流程类 6 条）+ **领域防错**（环境档案），领域特定结论不再污染主文档。
  5. 示例轮次与环境基线移出主文档 → `examples/`。
  6. 通用前置检查增「读环境档案」步骤；`pairs` 工作流输出增布局模式与档案状态列。
- **2026-09-03 v0.3.1**（补全语义定义）：
  1. **编号 `G-xxx` → `GapNNN`** —— 单字母 `G` 自 v0.1.0 起全程使用却从未定义（NAS 原版无此编号），且无法顾名思义。改为 `Gap` + **三位补零**序号（如 `Gap001`）：补零是防止台账破百后 `Gap10` 字符串排序排到 `Gap2` 前面；全局递增、关闭后不回收号。
  2. **补「批量条目为何合成一条」的理由** —— 同源同质（单一根因）+ 同批动作（状态同步推进）；逐篇登记会虚高 P0 计数、顶死收敛门槛，并把结构性存量缺口伪装成 N 个离散缺口。
  3. 台账条目增 **「子项完成度 x/y」** 字段（必修）：批量条目状态机只能整体推进，无此字段无法表达「40 篇消化了 15 篇」的中间态；满格后才允许置 `closed`。
  4. `templates/ledger-template.md` 增编号说明 + 单篇/批量**两种条目样例**（批量例含子项明细表）。

---

## 边界与免责（不重复造轮子）

- **复用**：llm-wiki 的 ingest/query/status、置信度标注、SHA256 缓存、anydoc 转换；nuwa-skill 的导师蒸馏与人物包结构；文献检索工具
- **独有增量**：Step 0 多库目标对发现、五路缺口聚合、覆盖率三级回退、ledger 状态机、反馈路由+去重、收敛判定（含覆盖率维度）、`board.md` 联合画像、`<库>/tutor/loop/` 跨会话状态、环境档案机制、通用防错清单
- **不做**：不替代 llm-wiki 的消化管线；不替代 nuwa-skill 的蒸馏；不激活/复制导师包；不自动修改思维模型本体
- **红线**：删除/覆盖知识库核心数据、修改库用途文档（`purpose.md` / `.wiki-schema.md`）等一律按库规拒绝；写回画像须带来源，无来源不写
